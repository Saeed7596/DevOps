# Delete the user's LDAP Identity
* `Admin Area -> Users -> Username -> Identities`
* Cpoy the `identity string`
* `Delete identity`
# Unblock User
* Click on 3 dots at the top right corner `unblock user`
# Add `identity string` again
